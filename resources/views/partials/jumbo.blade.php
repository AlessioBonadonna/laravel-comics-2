<section id="jumbo">
    @if (Route::currentRouteName() == 'comics.index')
        <button id="current" class="bg-primary">
            <h1>Current series </h1>
        </button>
    @endif

</section>
